$(function () {
    $('#wotuTable').DataTable()

    $('#baiduBtn').on('click', function () {

    })

    // 默认加载body体全部sloth图片
    var sloth = new Sloth();
    // 默认滚动条加载
    sloth.init();
    // 手动加载
    // sloth.load();
})
